Web app for storing and accessing keys anonimously

## Configuration

Create a `.env` file in the project root before running the app. You can copy the example file:

Then fill in your values:

```dotenv
ENV_NAME="Development"
BASE_URL="http://127.0.0.1:8000"
DB_URL="sqlite:///./shortener.db"
```
